From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: nostalfinals <yuu8583@proton.me>
Date: Fri, 28 Aug 2026 01:23:38 +0800
Subject: [PATCH] Implement Content System


diff --git a/src/main/java/io/papermc/paper/datacomponent/DataComponentAdapter.java b/src/main/java/io/papermc/paper/datacomponent/DataComponentAdapter.java
index 94573a05b97f99a66b238736db3156464b297dec..551fe36cd704478437ff05c21e88b31e407a88b2 100644
--- a/src/main/java/io/papermc/paper/datacomponent/DataComponentAdapter.java
+++ b/src/main/java/io/papermc/paper/datacomponent/DataComponentAdapter.java
@@ -27,9 +27,17 @@ public record DataComponentAdapter<NMS, API>(
     }
 
     public NMS toVanilla(final API value, final Holder<? extends DataComponentType<NMS>> type) {
+        // Nix start - support component conversion during registry composition
+        return this.toVanilla(value, type, new net.minecraft.resources.RegistryOps.HolderLookupAdapter(CraftRegistry.getMinecraftRegistry()));
+    }
+
+    public NMS toVanilla(final API value, final Holder<? extends DataComponentType<NMS>> type, final net.minecraft.resources.RegistryOps.RegistryInfoLookup lookup) {
+        // Nix end - support component conversion during registry composition
         final NMS nms = this.apiToVanilla.apply(value);
         if (this.codecValidation && !type.value().isTransient()) {
-            type.value().codecOrThrow().encodeStart(CraftRegistry.getMinecraftRegistry().createSerializationContext(NullOps.INSTANCE), nms).ifError(error -> {
+            // Nix start - validate against the caller's registry context
+            type.value().codecOrThrow().encodeStart(net.minecraft.resources.RegistryOps.create(NullOps.INSTANCE, lookup), nms).ifError(error -> {
+            // Nix end - validate against the caller's registry context
                 throw new IllegalArgumentException("Failed to encode data component %s (%s)".formatted(type.unwrapKey().orElseThrow(), error.message()));
             });
         }
diff --git a/src/main/java/io/papermc/paper/datacomponent/DataComponentAdapters.java b/src/main/java/io/papermc/paper/datacomponent/DataComponentAdapters.java
index a775db74a4479687f0c93115b6dc62a3a44e0190..92739e639f4072d46bcb6720d4ba7c24a01483c3 100644
--- a/src/main/java/io/papermc/paper/datacomponent/DataComponentAdapters.java
+++ b/src/main/java/io/papermc/paper/datacomponent/DataComponentAdapters.java
@@ -254,4 +254,13 @@ public final class DataComponentAdapters {
         }
         ADAPTERS.put(key, new DataComponentAdapter<>(apiToVanilla, vanillaToApi, codecValidation));
     }
+
+    // Nix start - provide identity adapters for Content System component values
+    public static DataComponentAdapter<?, ?> createContentSystemAdapter(final boolean valued) {
+        if (valued) {
+            return new DataComponentAdapter<>(Function.identity(), Function.identity(), false);
+        }
+        return new DataComponentAdapter<>(DataComponentAdapter.API_TO_UNIT_CONVERTER, ignored -> null, false);
+    }
+    // Nix end - provide identity adapters for Content System component values
 }
diff --git a/src/main/java/io/papermc/paper/datacomponent/PaperDataComponentType.java b/src/main/java/io/papermc/paper/datacomponent/PaperDataComponentType.java
index 8b123740e8881ba69af4e772c5d9be82c4d9c813..3a6e8cb30b2f66bb33a088fc16e577fcd8b51696 100644
--- a/src/main/java/io/papermc/paper/datacomponent/PaperDataComponentType.java
+++ b/src/main/java/io/papermc/paper/datacomponent/PaperDataComponentType.java
@@ -1,12 +1,12 @@
 package io.papermc.paper.datacomponent;
 
+import club.plutoproject.nix.contentsystem.registry.CustomDataComponentType;
 import io.papermc.paper.registry.HolderableBase;
 import java.util.Collections;
 import java.util.HashSet;
 import java.util.Set;
 import net.minecraft.core.Holder;
 import net.minecraft.core.component.DataComponentGetter;
-import net.minecraft.core.component.DataComponentMap;
 import net.minecraft.core.registries.Registries;
 import org.bukkit.craftbukkit.CraftRegistry;
 import org.jspecify.annotations.Nullable;
@@ -51,6 +51,11 @@ public abstract class PaperDataComponentType<T, NMS> extends HolderableBase<net.
 
     @Override
     public boolean isPersistent() {
+        // Nix start - persistence belongs to Content System metadata, not the vanilla codec
+        if (this.getHandle() instanceof CustomDataComponentType<?> custom) {
+            return custom.persistenceCodec() != null;
+        }
+        // Nix end - persistence belongs to Content System metadata, not the vanilla codec
         return !this.getHandle().isTransient();
     }
 
@@ -60,7 +65,12 @@ public abstract class PaperDataComponentType<T, NMS> extends HolderableBase<net.
 
     @SuppressWarnings({"unchecked"})
     public static <NMS> DataComponentType of(final Holder<?> holder) {
-        final DataComponentAdapter<NMS, ?> adapter = (DataComponentAdapter<NMS, ?>) DataComponentAdapters.ADAPTERS.get(holder.unwrapKey().orElseThrow());
+        // Nix start - resolve Content System component adapters from the registered value
+        DataComponentAdapter<NMS, ?> adapter = (DataComponentAdapter<NMS, ?>) DataComponentAdapters.ADAPTERS.get(holder.unwrapKey().orElseThrow());
+        if (adapter == null && holder.value() instanceof CustomDataComponentType<?> custom) {
+            adapter = (DataComponentAdapter<NMS, ?>) custom.paperAdapter();
+        }
+        // Nix end - resolve Content System component adapters from the registered value
         if (adapter == null) {
             throw new IllegalArgumentException("No adapter found for " + holder);
         }
diff --git a/src/main/java/io/papermc/paper/registry/PaperRegistries.java b/src/main/java/io/papermc/paper/registry/PaperRegistries.java
index 17cf4ecbd97bf655a61a476119b7a1a00996aafd..bc849fad0021e6260594c82de03e43ce7dba8628 100644
--- a/src/main/java/io/papermc/paper/registry/PaperRegistries.java
+++ b/src/main/java/io/papermc/paper/registry/PaperRegistries.java
@@ -118,7 +118,9 @@ public final class PaperRegistries {
             start(Registries.STRUCTURE_TYPE, RegistryKey.STRUCTURE_TYPE).craft(StructureType.class, CraftStructureType::new).build(),
             start(Registries.MOB_EFFECT, RegistryKey.MOB_EFFECT).craft(PotionEffectType.class, CraftPotionEffectType::new).build(),
             start(Registries.BLOCK, RegistryKey.BLOCK).craft(BlockType.class, CraftBlockType::new).build(),
-            start(Registries.ITEM, RegistryKey.ITEM).craft(ItemType.class, CraftItemType::new).build(),
+            // Nix start - add Content System items
+            start(Registries.ITEM, RegistryKey.ITEM).craft(ItemType.class, CraftItemType::new).addable(club.plutoproject.nix.contentsystem.registry.CustomItemRegistryEntry.PaperBuilder::new),
+            // Nix end - add Content System items
             start(Registries.VILLAGER_PROFESSION, RegistryKey.VILLAGER_PROFESSION).craft(Villager.Profession.class, CraftVillager.CraftProfession::new).build(),
             start(Registries.VILLAGER_TYPE, RegistryKey.VILLAGER_TYPE).craft(Villager.Type.class, CraftVillager.CraftType::new).build(),
             start(Registries.MAP_DECORATION_TYPE, RegistryKey.MAP_DECORATION_TYPE).craft(MapCursor.Type.class, CraftMapCursor.CraftType::new).build(),
@@ -126,7 +128,9 @@ public final class PaperRegistries {
             start(Registries.ATTRIBUTE, RegistryKey.ATTRIBUTE).craft(Attribute.class, CraftAttribute::new).serializationUpdater(FieldRename.ATTRIBUTE_RENAME).build(),
             start(Registries.FLUID, RegistryKey.FLUID).craft(Fluid.class, CraftFluid::new).build(),
             start(Registries.SOUND_EVENT, RegistryKey.SOUND_EVENT).craft(Sound.class, CraftSound::new, true).create(PaperSoundEventRegistryEntry.PaperBuilder::new, RegistryEntryMeta.RegistryModificationApiSupport.NONE),
-            start(Registries.DATA_COMPONENT_TYPE, RegistryKey.DATA_COMPONENT_TYPE).craft(DataComponentTypes.class, PaperDataComponentType::of).build(),
+            // Nix start - add Content System data components
+            start(Registries.DATA_COMPONENT_TYPE, RegistryKey.DATA_COMPONENT_TYPE).craft(DataComponentTypes.class, PaperDataComponentType::of).addable(club.plutoproject.nix.contentsystem.registry.CustomDataComponentRegistryEntry.PaperBuilder::new),
+            // Nix end - add Content System data components
             start(Registries.GAME_RULE, RegistryKey.GAME_RULE).craft(GameRule.class, CraftGameRule::new).build(),
             start(Registries.POINT_OF_INTEREST_TYPE, RegistryKey.POINT_OF_INTEREST_TYPE).craft(PoiTypes.class, PaperPoiType::new).build(),
 
diff --git a/src/main/java/io/papermc/paper/registry/WritableCraftRegistry.java b/src/main/java/io/papermc/paper/registry/WritableCraftRegistry.java
index 9e8672c705eb27c9b7d190129129454fd83cf7d5..a82195f17a4f08010aaaacaa8bd7ffc4b67831c2 100644
--- a/src/main/java/io/papermc/paper/registry/WritableCraftRegistry.java
+++ b/src/main/java/io/papermc/paper/registry/WritableCraftRegistry.java
@@ -30,7 +30,13 @@ public class WritableCraftRegistry<M, T extends Keyed, B extends PaperRegistryBu
 
     public void register(final TypedKey<T> key, final Consumer<RegistryBuilderFactory<T, B>> value, final Conversions conversions) {
         final ResourceKey<M> resourceKey = PaperRegistries.toNms(key);
+        // Nix start - scope custom builder keys and reject duplicates before builders have side effects
         this.registry.validateWrite(resourceKey);
+        if (this.registry.get(resourceKey).isPresent()) {
+            throw new IllegalStateException("Adding duplicate key '" + resourceKey + "' to registry");
+        }
+        try (final club.plutoproject.nix.contentsystem.registry.RegistrationKeyScope.Scope ignored = club.plutoproject.nix.contentsystem.registry.RegistrationKeyScope.enter(resourceKey)) {
+        // Nix end - scope custom builder keys and reject duplicates before builders have side effects
         final PaperRegistryBuilderFactory<M, T, B> builderFactory = new PaperRegistryBuilderFactory<>(this.registry.key(), conversions, this.meta.builderFiller(), this.registry::getValueForCopying);
         value.accept(builderFactory);
         PaperRegistryListenerManager.INSTANCE.registerWithListeners(
@@ -41,6 +47,9 @@ public class WritableCraftRegistry<M, T extends Keyed, B extends PaperRegistryBu
             FROM_PLUGIN,
             conversions
         );
+        // Nix start - close custom builder key scope
+        }
+        // Nix end - close custom builder key scope
     }
 
     public WritableRegistry<T, B> createApiWritableRegistry(final Conversions conversions) {
diff --git a/src/main/java/io/papermc/paper/util/MCUtil.java b/src/main/java/io/papermc/paper/util/MCUtil.java
index f659aba1587d70b8d4011e66f40f480ff4deeeb4..fd4d3bd4df4485b5f7e18429646e7c21e0a390d3 100644
--- a/src/main/java/io/papermc/paper/util/MCUtil.java
+++ b/src/main/java/io/papermc/paper/util/MCUtil.java
@@ -290,9 +290,11 @@ public final class MCUtil {
         if (tag.getStringOr("id", "minecraft:air").equals("minecraft:air")) {
             return CraftItemStack.asCraftMirror(ItemStack.EMPTY);
         }
-        return CraftItemStack.asCraftMirror(ItemStack.CODEC.parse(
+        // Nix start - recover Content System metadata after vanilla decoding
+        return CraftItemStack.asCraftMirror(club.plutoproject.nix.contentsystem.ContentRuntime.current().recover(ItemStack.CODEC.parse(
             CraftRegistry.getMinecraftRegistry().createSerializationContext(NbtOps.INSTANCE), tag
-        ).getOrThrow());
+        ).getOrThrow()));
+        // Nix end - recover Content System metadata after vanilla decoding
     }
 
     public static byte[] serializeTagToBytes(final CompoundTag tag) {
diff --git a/src/main/java/org/bukkit/craftbukkit/CraftServer.java b/src/main/java/org/bukkit/craftbukkit/CraftServer.java
index 716c3b17dc07eb2417dfb0084fe75dbd270f031a..c6925aec05f4ca1c471d2ad4c89cb4b0009f3788 100644
--- a/src/main/java/org/bukkit/craftbukkit/CraftServer.java
+++ b/src/main/java/org/bukkit/craftbukkit/CraftServer.java
@@ -304,6 +304,9 @@ public final class CraftServer implements Server {
     private final io.papermc.paper.potion.PaperPotionBrewer potionBrewer;
     public final io.papermc.paper.SparksFly spark;
     private final ServerConfiguration serverConfig = new PaperServerConfiguration();
+    // Nix start - expose the server-owned Content System service
+    private final club.plutoproject.nix.contentsystem.ContentSystemImpl contentSystem;
+    // Nix end - expose the server-owned Content System service
 
     // Paper start - Folia region threading API
     private final io.papermc.paper.threadedregions.scheduler.FallbackRegionScheduler regionizedScheduler = new io.papermc.paper.threadedregions.scheduler.FallbackRegionScheduler();
@@ -413,6 +416,10 @@ public final class CraftServer implements Server {
          // Paper end
 
         CraftRegistry.setMinecraftRegistry(console.registryAccess());
+        // Nix start - initialize Content System after the server registry access exists
+        this.contentSystem = new club.plutoproject.nix.contentsystem.ContentSystemImpl();
+        club.plutoproject.nix.contentsystem.ContentRuntime.install(this.contentSystem);
+        // Nix end - initialize Content System after the server registry access exists
 
         if (!Main.useConsole) {
             this.getLogger().info("Console input is disabled due to --noconsole command argument");
@@ -819,6 +826,13 @@ public final class CraftServer implements Server {
         return this.serverTickManager;
     }
 
+    // Nix start - expose Content System through CraftServer
+    @Override
+    public club.plutoproject.nix.contentsystem.ContentSystem getContentSystem() {
+        return this.contentSystem;
+    }
+    // Nix end - expose Content System through CraftServer
+
     @Override
     public ResourcePack getServerResourcePack() {
         return this.getServer().getServerResourcePack().map(CraftResourcePack::new).orElse(null);
diff --git a/src/main/java/org/bukkit/craftbukkit/inventory/CraftItemFactory.java b/src/main/java/org/bukkit/craftbukkit/inventory/CraftItemFactory.java
index 70e21e1fc337c6ea40e64020fa1832d1630cae97..b30fbd252f68331d6ff0dc6e3c7f842fd33ae146 100644
--- a/src/main/java/org/bukkit/craftbukkit/inventory/CraftItemFactory.java
+++ b/src/main/java/org/bukkit/craftbukkit/inventory/CraftItemFactory.java
@@ -62,6 +62,12 @@ public final class CraftItemFactory implements ItemFactory {
         if (itemstack == null) {
             return false;
         }
+        // Nix start - specialized meta is never applicable to a custom Item Type
+        if (itemstack.getItemType() instanceof CraftItemType<?> type
+            && type.getHandle() instanceof club.plutoproject.nix.contentsystem.item.CustomItem) {
+            return meta instanceof CraftMetaItem && meta.getClass() == CraftMetaItem.class;
+        }
+        // Nix end - specialized meta is never applicable to a custom Item Type
         return this.isApplicable(meta, itemstack.getType());
     }
 
@@ -134,6 +140,13 @@ public final class CraftItemFactory implements ItemFactory {
     @Override
     public ItemMeta asMetaFor(ItemMeta meta, ItemStack stack) {
         Preconditions.checkArgument(stack != null, "ItemStack stack cannot be null");
+        // Nix start - retain generic ItemMeta for Content System items
+        if (stack.getItemType() instanceof CraftItemType<?> type
+            && type.getHandle() instanceof club.plutoproject.nix.contentsystem.item.CustomItem) {
+            Preconditions.checkArgument(meta instanceof CraftMetaItem, "Meta of %s not created by %s", (meta != null ? meta.getClass().toString() : "null"), CraftItemFactory.class.getName());
+            return meta.getClass() == CraftMetaItem.class ? meta : null;
+        }
+        // Nix end - retain generic ItemMeta for Content System items
         return this.asMetaFor(meta, stack.getType());
     }
 
@@ -157,7 +170,9 @@ public final class CraftItemFactory implements ItemFactory {
             if (reader.canRead()) {
                 throw new IllegalArgumentException("Trailing input found when parsing ItemStack: " + reader.getRemaining());
             }
-            return CraftItemStack.asCraftMirror(in.createItemStack(1));
+            // Nix start - recover Content System metadata from parsed item input
+            return CraftItemStack.asCraftMirror(club.plutoproject.nix.contentsystem.ContentRuntime.current().recover(in.createItemStack(1)));
+            // Nix end - recover Content System metadata from parsed item input
         } catch (CommandSyntaxException ex) {
             throw new IllegalArgumentException("Could not parse ItemStack: " + input, ex);
         }
diff --git a/src/main/java/org/bukkit/craftbukkit/inventory/CraftItemMetas.java b/src/main/java/org/bukkit/craftbukkit/inventory/CraftItemMetas.java
index 3aac6831f5f3b7c5d6ca13110f88b2ad5189ced7..64d0f471c3c754eb5b09dc493cddbb3ecc973666 100644
--- a/src/main/java/org/bukkit/craftbukkit/inventory/CraftItemMetas.java
+++ b/src/main/java/org/bukkit/craftbukkit/inventory/CraftItemMetas.java
@@ -163,6 +163,11 @@ public final class CraftItemMetas {
         if (itemType == ItemType.AIR) {
             return CraftItemMetas.asType(CraftItemMetas.EMPTY_META_DATA);
         }
+        // Nix start - custom item types are never specialized by Material
+        if (itemHandle instanceof club.plutoproject.nix.contentsystem.item.CustomItem) {
+            return CraftItemMetas.asType(CraftItemMetas.ITEM_META_DATA);
+        }
+        // Nix end - custom item types are never specialized by Material
         if (itemType == ItemType.WRITTEN_BOOK) { // sync with CraftMetaBookSigned#applicableTo
             return CraftItemMetas.asType(CraftItemMetas.SIGNED_BOOK_META_DATA);
         }
diff --git a/src/main/java/org/bukkit/craftbukkit/inventory/CraftItemStack.java b/src/main/java/org/bukkit/craftbukkit/inventory/CraftItemStack.java
index 3eea7b5e12a440162ca07113a59cd5ccacf0dd2d..439f13938096d7385083404df454be59993f73a7 100644
--- a/src/main/java/org/bukkit/craftbukkit/inventory/CraftItemStack.java
+++ b/src/main/java/org/bukkit/craftbukkit/inventory/CraftItemStack.java
@@ -196,6 +196,11 @@ public final class CraftItemStack extends ItemStack {
     }
 
     public static CraftItemStack asNewCraftStack(Item item, int amount) {
+        // Nix start - retain custom Item Type identity when creating a Craft mirror
+        if (item instanceof club.plutoproject.nix.contentsystem.item.CustomItem) {
+            return CraftItemStack.asCraftMirror(new net.minecraft.world.item.ItemStack(item, amount));
+        }
+        // Nix end - retain custom Item Type identity when creating a Craft mirror
         return new CraftItemStack(CraftItemType.minecraftToBukkit(item), amount, (short) 0, null);
     }
 
@@ -218,9 +223,14 @@ public final class CraftItemStack extends ItemStack {
         this.handle = item;
     }
 
+    // Nix start - preserve actual Item Type in Craft copies
     private CraftItemStack(ItemStack item) {
-        this(item.getType(), item.getAmount(), item.getDurability(), item.hasItemMeta() ? item.getItemMeta() : null);
+        this(CraftItemStack.asNMSCopy(item));
+        if (this.handle != null && this.handle.isEmpty()) {
+            this.handle = null;
+        }
     }
+    // Nix end - preserve actual Item Type in Craft copies
 
     private CraftItemStack(Material type, int amount, short durability, ItemMeta itemMeta) {
         this.setType(type);
@@ -239,25 +249,36 @@ public final class CraftItemStack extends ItemStack {
         return this.handle != null ? CraftItemType.minecraftToBukkit(this.handle.getItem()) : Material.AIR;
     }
 
+    // Nix start - expose actual registry identity separately from Material compatibility
+    @Override
+    public ItemType getItemType() {
+        return this.handle == null || this.handle.isEmpty()
+            ? ItemType.AIR
+            : CraftItemType.minecraftToBukkitNew(this.handle.getItem());
+    }
+    // Nix end
+
     @Override
     public void setType(Material type) {
-        if (this.getType() == type) {
-            return;
-        } else if (type == Material.AIR) {
-            this.handle = null;
-        } else if (CraftItemType.bukkitToMinecraft(type) == null) { // :(
+        // Nix start - mutate by actual Item identity, not the compatibility Material
+        Preconditions.checkArgument(type != null, "Material cannot be null");
+        final Item target = CraftItemType.bukkitToMinecraft(type);
+        if (type == Material.AIR || target == null) {
             this.handle = null;
         } else if (this.handle == null) {
-            this.handle = new net.minecraft.world.item.ItemStack(CraftItemType.bukkitToMinecraft(type), 1);
-        } else {
-            final Material oldType = CraftMagicNumbers.getMaterial(this.handle.getItem()); // Paper
-            this.handle.setItem(CraftItemType.bukkitToMinecraft(type));
-            if (this.hasItemMeta()) {
+            this.handle = new net.minecraft.world.item.ItemStack(target, 1);
+        } else if (this.handle.getItem() != target) {
+            final Material oldType = CraftMagicNumbers.getMaterial(this.handle.getItem());
+            this.handle.setItem(target);
+            if (this.hasItemMeta()
+                && !(this.handle.getItem() instanceof club.plutoproject.nix.contentsystem.item.CustomItem)
+                && !(target instanceof club.plutoproject.nix.contentsystem.item.CustomItem)) {
                 // This will create the appropriate item meta, which will contain all the data we intend to keep
                 this.adjustTagForItemMeta(oldType); // Paper
             }
         }
         this.setData((MaterialData) null); // Paper
+        // Nix end - mutate by actual Item identity, not the compatibility Material
     }
 
     @Override
@@ -300,12 +321,14 @@ public final class CraftItemStack extends ItemStack {
     public byte @NotNull [] serializeAsBytes() {
         Preconditions.checkArgument(!this.isEmpty(), "Empty item cannot be serialized");
 
+        // Nix start - write the vanilla-loadable Persistent Form instead of a custom top-level stack
         return MCUtil.serializeTagToBytes(
             (CompoundTag) net.minecraft.world.item.ItemStack.CODEC.encodeStart(
                 CraftRegistry.getMinecraftRegistry().createSerializationContext(NbtOps.INSTANCE),
-                this.handle
+                club.plutoproject.nix.contentsystem.ContentRuntime.current().persistentForm(this.handle)
             ).getOrThrow()
         );
+        // Nix end - write the vanilla-loadable Persistent Form instead of a custom top-level stack
     }
 
     @Override
@@ -382,10 +405,12 @@ public final class CraftItemStack extends ItemStack {
         if (this.isEmpty()) {
             return Map.of("id", "minecraft:air", SharedConstants.DATA_VERSION_TAG, Bukkit.getUnsafe().getDataVersion(), "schema_version", 1);
         }
+        // Nix start - serialize the vanilla-loadable Persistent Form
         final CompoundTag tag = (CompoundTag) net.minecraft.world.item.ItemStack.CODEC.encodeStart(
             CraftRegistry.getMinecraftRegistry().createSerializationContext(NbtOps.INSTANCE),
-            CraftItemStack.asNMSCopy(this)
+            club.plutoproject.nix.contentsystem.ContentRuntime.current().persistentForm(CraftItemStack.asNMSCopy(this))
         ).getOrThrow();
+        // Nix end - serialize the vanilla-loadable Persistent Form
         NbtUtils.addCurrentDataVersion(tag);
 
         final Map<String, Object> ret = new LinkedHashMap<>();
@@ -472,6 +497,11 @@ public final class CraftItemStack extends ItemStack {
     }
     public static ItemMeta getItemMeta(net.minecraft.world.item.ItemStack item, ItemType metaForType, final Set<DataComponentType<?>> extraHandledComponents) {
         // Paper end - handled tags on type change
+        // Nix start - custom types always expose generic ItemMeta, even without a patch
+        if (item != null && item.getItem() instanceof club.plutoproject.nix.contentsystem.item.CustomItem) {
+            return ((CraftItemType<?>) CraftItemType.minecraftToBukkitNew(item.getItem())).getItemMeta(item, extraHandledComponents);
+        }
+        // Nix end - custom types always expose generic ItemMeta, even without a patch
         if (!CraftItemStack.hasItemMeta(item)) {
             return CraftItemFactory.instance().getItemMeta(CraftItemStack.getType(item));
         }
@@ -493,6 +523,26 @@ public final class CraftItemStack extends ItemStack {
         if (item == null) {
             return false;
         }
+        // Nix start - Content System items accept only the generic component-backed meta
+        if (item.getItem() instanceof club.plutoproject.nix.contentsystem.item.CustomItem) {
+            if (CraftItemFactory.instance().equals(itemMeta, null)) {
+                item.restorePatch(DataComponentPatch.EMPTY);
+                return true;
+            }
+            if (!(itemMeta instanceof CraftMetaItem) || itemMeta.getClass() != CraftMetaItem.class) {
+                return false;
+            }
+            if (!((CraftMetaItem) itemMeta).isEmpty()) {
+                final CraftMetaItem.Applicator applicator = new CraftMetaItem.Applicator() {};
+                ((CraftMetaItem) itemMeta).applyToItem(applicator);
+                item.restorePatch(DataComponentPatch.EMPTY);
+                item.applyComponents(applicator.build());
+            } else {
+                item.restorePatch(DataComponentPatch.EMPTY);
+            }
+            return true;
+        }
+        // Nix end - Content System items accept only the generic component-backed meta
         if (CraftItemFactory.instance().equals(itemMeta, null)) {
             item.restorePatch(DataComponentPatch.EMPTY);
             return true;
@@ -563,24 +613,31 @@ public final class CraftItemStack extends ItemStack {
         return !(item == null || item.getComponentsPatch().isEmpty());
     }
     // Paper start - with type
+    // Nix start - convert by actual Item Type identity while retaining the component patch
     @Override
     public ItemStack withType(final Material type) {
-        if (type == Material.AIR) {
+        Preconditions.checkArgument(type != null, "Material cannot be null");
+        final ItemType itemType = type.asItemType();
+        if (itemType == null) {
             return CraftItemStack.asCraftMirror(null);
         }
+        return this.withType(itemType);
+    }
 
-        final net.minecraft.world.item.ItemStack copy = new net.minecraft.world.item.ItemStack(
-            CraftItemType.bukkitToMinecraft(type), this.getAmount()
-        );
-
+    @Override
+    public ItemStack withType(final ItemType type) {
+        Preconditions.checkArgument(type != null, "ItemType cannot be null");
+        final Item target = CraftItemType.bukkitToMinecraftNew(type);
+        if (target == Items.AIR) {
+            return CraftItemStack.asCraftMirror(null);
+        }
+        final net.minecraft.world.item.ItemStack copy = new net.minecraft.world.item.ItemStack(target, this.getAmount());
         if (this.handle != null) {
             copy.applyComponents(this.handle.getComponentsPatch());
         }
-
-        final CraftItemStack mirrored = CraftItemStack.asCraftMirror(copy);
-        mirrored.setItemMeta(mirrored.getItemMeta());
-        return mirrored;
+        return CraftItemStack.asCraftMirror(copy);
     }
+    // Nix end
     // Paper end
 
     @Override
diff --git a/src/main/java/org/bukkit/craftbukkit/inventory/CraftItemType.java b/src/main/java/org/bukkit/craftbukkit/inventory/CraftItemType.java
index b1f7fbf5f534bf30a3fd49d4f62a7874395f995b..04ffc2f2f673b1c00784cef6cd40817bfd90baae 100644
--- a/src/main/java/org/bukkit/craftbukkit/inventory/CraftItemType.java
+++ b/src/main/java/org/bukkit/craftbukkit/inventory/CraftItemType.java
@@ -44,7 +44,11 @@ public class CraftItemType<M extends ItemMeta> extends HolderableBase<Item> impl
     private final Supplier<CraftItemMetas.ItemMetaData<M>> itemMetaData;
 
     public static Material minecraftToBukkit(Item item) {
-        return CraftMagicNumbers.getMaterial(item);
+        // Nix start - expose a custom item's configured Material compatibility view
+        return item instanceof club.plutoproject.nix.contentsystem.item.CustomItem custom
+            ? custom.vanillaMaterial()
+            : CraftMagicNumbers.getMaterial(item);
+        // Nix end - expose a custom item's configured Material compatibility view
     }
 
     public static Item bukkitToMinecraft(Material material) {
@@ -137,6 +141,12 @@ public class CraftItemType<M extends ItemMeta> extends HolderableBase<Item> impl
         if (this == ItemType.AIR) {
             throw new UnsupportedOperationException("Air does not have ItemMeta");
         }
+        // Nix start - custom items always use the generic ItemMeta contract
+        if (this.getHandle() instanceof club.plutoproject.nix.contentsystem.item.CustomItem) {
+            @SuppressWarnings("unchecked") final Class<M> generic = (Class<M>) ItemMeta.class;
+            return generic;
+        }
+        // Nix end - custom items always use the generic ItemMeta contract
         return this.itemMetaData.get().metaClass();
     }
 
@@ -237,6 +247,11 @@ public class CraftItemType<M extends ItemMeta> extends HolderableBase<Item> impl
 
     @Override
     public @Nullable Material asMaterial() {
+        // Nix start - custom item types retain their configured Material mapping
+        if (this.getHandle() instanceof club.plutoproject.nix.contentsystem.item.CustomItem custom) {
+            return custom.vanillaMaterial();
+        }
+        // Nix end - custom item types retain their configured Material mapping
         return Registry.MATERIAL.get(this.getKey());
     }
 
diff --git a/src/main/java/org/bukkit/craftbukkit/legacy/MaterialRerouting.java b/src/main/java/org/bukkit/craftbukkit/legacy/MaterialRerouting.java
index a76e89c12afdf68aadc2742136d008a3f1b2e9aa..931d75051b0559aa8f901fc3671ecf9245f0335f 100644
--- a/src/main/java/org/bukkit/craftbukkit/legacy/MaterialRerouting.java
+++ b/src/main/java/org/bukkit/craftbukkit/legacy/MaterialRerouting.java
@@ -671,6 +671,22 @@ public class MaterialRerouting {
     public static ItemStack withType(final ItemStack itemStack, final Material material) {
         return itemStack.withType(material);
     }
+
+    // Nix start - reroute Content System Material compatibility methods
+    public static club.plutoproject.nix.contentsystem.registry.ItemTypeRegistryEntry.Builder vanillaMaterial(
+        final club.plutoproject.nix.contentsystem.registry.ItemTypeRegistryEntry.Builder builder,
+        final Material material
+    ) {
+        return builder.vanillaMaterial(MaterialRerouting.transformToItemType(material));
+    }
+
+    public static Material vanillaMaterial(
+        final club.plutoproject.nix.contentsystem.projection.ProjectionOutput output,
+        @InjectPluginVersion ApiVersion version
+    ) {
+        return MaterialRerouting.transformFromItemType(output.vanillaMaterial(), version);
+    }
+    // Nix end - reroute Content System Material compatibility methods
     // Paper end - register paper API specific material consumers in rerouting
 
     // Paper start - methods added post 1.13, no-op
diff --git a/src/main/java/org/bukkit/craftbukkit/util/CraftMagicNumbers.java b/src/main/java/org/bukkit/craftbukkit/util/CraftMagicNumbers.java
index 80317dc41f85b91d7219f370d6f51e79496b09c4..81ec32d840e4edce12650069e1a7cf5022b17990 100644
--- a/src/main/java/org/bukkit/craftbukkit/util/CraftMagicNumbers.java
+++ b/src/main/java/org/bukkit/craftbukkit/util/CraftMagicNumbers.java
@@ -170,6 +170,11 @@ public final class CraftMagicNumbers implements UnsafeValues {
     }
 
     public static Material getMaterial(Item item) {
+        // Nix start - preserve the configured Material compatibility for custom items
+        if (item instanceof club.plutoproject.nix.contentsystem.item.CustomItem custom) {
+            return custom.vanillaMaterial();
+        }
+        // Nix end - preserve the configured Material compatibility for custom items
         return CraftMagicNumbers.ITEM_MATERIAL.getOrDefault(item, Material.AIR);
     }
 
@@ -527,7 +532,12 @@ public final class CraftMagicNumbers implements UnsafeValues {
         // Serialize as SNBT to preserve exact NBT types; vanilla codecs already can handle such deserialization.
         net.minecraft.world.item.component.CustomData.SERIALIZE_CUSTOM_AS_SNBT.set(true);
         try {
-            item = net.minecraft.world.item.ItemStack.CODEC.encodeStart(ops, CraftItemStack.unwrap(itemStack)).getOrThrow().getAsJsonObject();
+            // Nix start - serialize the vanilla-loadable Persistent Form
+            item = net.minecraft.world.item.ItemStack.CODEC.encodeStart(
+                ops,
+                club.plutoproject.nix.contentsystem.ContentRuntime.current().persistentForm(CraftItemStack.unwrap(itemStack))
+            ).getOrThrow().getAsJsonObject();
+            // Nix end - serialize the vanilla-loadable Persistent Form
         } finally {
             net.minecraft.world.item.component.CustomData.SERIALIZE_CUSTOM_AS_SNBT.set(false);
         }
@@ -543,7 +553,9 @@ public final class CraftMagicNumbers implements UnsafeValues {
         final int currentVersion = this.getDataVersion();
         data = (com.google.gson.JsonObject) MinecraftServer.getServer().getFixerUpper().update(References.ITEM_STACK, new Dynamic<>(com.mojang.serialization.JsonOps.INSTANCE, data), dataVersion, currentVersion).getValue();
         com.mojang.serialization.DynamicOps<com.google.gson.JsonElement> ops = CraftRegistry.getMinecraftRegistry().createSerializationContext(com.mojang.serialization.JsonOps.INSTANCE);
-        return CraftItemStack.asCraftMirror(net.minecraft.world.item.ItemStack.CODEC.parse(ops, data).getOrThrow(IllegalArgumentException::new));
+        // Nix start - recover Content System metadata after vanilla JSON decoding
+        return CraftItemStack.asCraftMirror(club.plutoproject.nix.contentsystem.ContentRuntime.current().recover(net.minecraft.world.item.ItemStack.CODEC.parse(ops, data).getOrThrow(IllegalArgumentException::new)));
+        // Nix end - recover Content System metadata after vanilla JSON decoding
     }
 
     @Override
