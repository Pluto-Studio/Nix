From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: nostalfinals <yuu8583@proton.me>
Date: Sun, 26 Apr 2026 00:43:47 +0800
Subject: [PATCH] Avoid Optional allocation on mob spawn hot path


diff --git a/net/minecraft/util/random/WeightedList.java b/net/minecraft/util/random/WeightedList.java
index 9bb26ab7e7bedfa67b24897213093cbf2213469e..bc50a25cf85e3d75a2c2f0a70bbc329838ea858a 100644
--- a/net/minecraft/util/random/WeightedList.java
+++ b/net/minecraft/util/random/WeightedList.java
@@ -72,6 +72,17 @@ public class WeightedList<E> { // Paper - non-final
         }
     }
 
+    // Nix start - Avoid Optional allocation on mob spawn hot path
+    public @Nullable E getRandomOrNull(final RandomSource random) {
+        if (this.selector == null) {
+            return null;
+        } else {
+            int selection = random.nextInt(this.totalWeight);
+            return Objects.requireNonNull(this.selector.get(selection));
+        }
+    }
+    // Nix end - Avoid Optional allocation on mob spawn hot path
+
     public E getRandomOrThrow(RandomSource random) {
         if (this.selector == null) {
             throw new IllegalStateException("Weighted list has no elements");
diff --git a/net/minecraft/world/level/NaturalSpawner.java b/net/minecraft/world/level/NaturalSpawner.java
index 00414981aa51977437663697a7f2a145c97f1a5f..401da232e6794da6bc4675b9b51fe2add1be78b4 100644
--- a/net/minecraft/world/level/NaturalSpawner.java
+++ b/net/minecraft/world/level/NaturalSpawner.java
@@ -269,14 +269,16 @@ public final class NaturalSpawner {
                         double d2 = nearestPlayer.distanceToSqr(d, y, d1);
                         if (level.isLoadedAndInBounds(mutableBlockPos) && isRightDistanceToPlayerAndSpawnPoint(level, chunk, mutableBlockPos, d2)) { // Paper - don't load chunks for mob spawn
                             if (spawnerData == null) {
-                                Optional<MobSpawnSettings.SpawnerData> randomSpawnMobAt = getRandomSpawnMobAt(
+                                // Nix start - Avoid Optional allocation on mob spawn hot path
+                                final MobSpawnSettings.SpawnerData randomSpawnMobAt = getRandomSpawnMobAtOrNull(
                                     level, structureManager, generator, category, level.random, mutableBlockPos
                                 );
-                                if (randomSpawnMobAt.isEmpty()) {
+                                if (randomSpawnMobAt == null) {
                                     break;
                                 }
 
-                                spawnerData = randomSpawnMobAt.get();
+                                spawnerData = randomSpawnMobAt;
+                                // Nix end - Avoid Optional allocation on mob spawn hot path
                                 ceil = spawnerData.minCount() + level.random.nextInt(1 + spawnerData.maxCount() - spawnerData.minCount());
                             }
 
@@ -432,6 +434,22 @@ public final class NaturalSpawner {
             : mobsAt(level, structureManager, generator, category, pos, biome).getRandom(random);
     }
 
+    // Nix start - Avoid Optional allocation on mob spawn hot path
+    private static MobSpawnSettings.@Nullable SpawnerData getRandomSpawnMobAtOrNull(
+            final ServerLevel level,
+            final StructureManager structureManager,
+            final ChunkGenerator generator,
+            final MobCategory mobCategory,
+            final RandomSource random,
+            final BlockPos pos
+    ) {
+        Holder<Biome> biome = level.getBiome(pos);
+        return mobCategory == MobCategory.WATER_AMBIENT && biome.is(BiomeTags.REDUCED_WATER_AMBIENT_SPAWNS) && random.nextFloat() < 0.98F
+                ? null
+                : mobsAt(level, structureManager, generator, mobCategory, pos, biome).getRandomOrNull(random);
+    }
+    // Nix end - Avoid Optional allocation on mob spawn hot path
+
     private static boolean canSpawnMobAt(
         ServerLevel level, StructureManager structureManager, ChunkGenerator generator, MobCategory category, MobSpawnSettings.SpawnerData data, BlockPos pos
     ) {
